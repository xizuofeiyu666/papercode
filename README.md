Experimental Environment

The implementation is based on PyTorch 2.0.1 with CUDA 11.7. All experiments are conducted on a single NVIDIA RTX 4060 Ti GPU.

Training Settings

For all datasets, the input image resolution is uniformly set to 640 × 640. The weight decay is set to 0.0005, and the initial learning rate is set to 0.01.
